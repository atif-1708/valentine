export interface Memory {
  id: number;
  url: string;
  caption: string;
  rotation: number;
}

export interface ContractClause {
  id: number;
  text: string;
}

export interface Song {
  title: string;
  src: string;
}